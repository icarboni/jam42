using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TIC : MonoBehaviour
{
   /* public playerMovement pMScript;
    public void TicEject(Collision2D _other) => StartCoroutine("Tic", _other);

    IEnumerator Tic(Collision2D _other)
    {
        pMScript.stunned = true;
        yield return new WaitForSeconds(2);
        pMScript.stunned = false;
        yield return null;
    }*/
}
